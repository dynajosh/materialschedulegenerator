import initState from '../store/index'



const valueChangeHandler = (state = initState, action) => {
    state = valueChangeHandler // IDK, update this from the form
    return state
}

export default valueChangeHandler